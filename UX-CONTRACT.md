# 智能客服 Agent UX 合约

相关视觉规范见 [DESIGN.md](DESIGN.md)。业务行为来源是 [MVP 设计规格](docs/superpowers/specs/2026-09-13-smart-customer-service-design.md) 与 API 实现；本文件只记录跨页面必须一致的界面结果。

## 角色与路由

| 路由 | 角色 | 完成条件 |
| --- | --- | --- |
| `/chat` | 客户 | 获得流式回复、来源提示，或进入人工队列 |
| `/agent` | 坐席 | 接管工单、发送人工回复并关闭工单 |
| `/admin` | 运营 | 导入并索引知识，查看回放和评估 |

## 反馈与异步

- 成功使用 `success`、普通进度和状态使用 `info`、可恢复失败使用 `destructive`；失败 Alert 使用 `role="alert"`，其它提示使用 `role="status"`。
- 影响会话或工单状态的请求采用悲观更新：服务端确认成功前不宣称操作成功。
- 每个提交按钮在进行中禁用并使用其规范加载态，避免重复请求；请求结束后恢复可用。
- 网络或服务端失败必须保留尚未提交的文本，并提供原位重试途径；不显示敏感内部信息。

## 知识导入

- 只有文档建立索引成功后，清空标题与 Markdown 内容。
- 导入失败时保留已填写内容、显示失败原因，用户可直接再次提交。
- 成功后刷新知识列表并给出成功反馈。

## 工单关闭

- 关闭工单仅在服务端确认后退出当前处理视图并显示“工单已解决”。
- 关闭失败时仍保留当前工单与上下文，显示失败提示，并重新启用“关闭工单”以便重试。

## 可访问性

- 所有操作使用原生按钮或组件库按钮，并有可读名称。
- 表单关闭浏览器原生校验气泡，失败反馈通过语义 live region 宣告。
- 键盘 Enter 可提交文本输入；进行中与禁用状态必须可感知。
